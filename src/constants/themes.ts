import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
    colors: {
        color : {
        danger : 'rgb(255,0,0)',
        dark : 'rgb(0, 0, 0)',
        darkMode : '#2e2c2c',
        lightMode : '#f9f6f6',
        lightHover : '#f3f0f0',
        highLight : "#9B1FE9",
        secondaryButton : "#2D797B"
        }
    },
});